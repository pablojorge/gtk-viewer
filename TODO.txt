Bugs:
 - When the filelist is filtered and an item is put back (after an undo)
   it may end up misplaced in the original (non-filtered) list

Enhancements:
 + Asynchronous loading of images
 + Adjust image size when image is rotated
 + Use tempfile to extract thumbnails in pdffile and videofile
 - Fix accelerators handled manually with key bindings (Left, Right, Esc, V)
 - Support for delete / undelete in Mac OS X
 - Application name and icon

Features:
 + Embedded mode for PDF files (open in a separate viewer window? tempdir, remove all on exit)
 Gallery:
   + Directory selector mode (auto select dirs with no dirs in it)
   + New folder
   - Invalidate cache entries when files are moved/dirs created
 - Extract and apply JPEG image rotation metadata (http://tilloy.net/dev/pyexiv2/, http://www.pythonware.com/products/pil/)
 - Show images metadata
 - Support for copying files
 - Integrate metadata manipulation for MP3s/PDFs/EPubs (show, edit, autogen)
 - Undo pane
 - Log pane

